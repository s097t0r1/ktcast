package ${packageName}.api

import androidx.fragment.app.Fragment
import me.s097t0r1.core.navigation.screen.FragmentScreen

interface ${__formattedModuleName}FeatureStarter {
    fun start(): FragmentScreen<Fragment>
}
